# Expo SDK 53: expo-router のオプショナルピア react-server-dom-webpack は
# react 19.0.0 と整合するバージョンが存在せず、npm arborist の既知バグにより
# 「配置不可能なオプショナルピア」が ERESOLVE エラーになるため必須。
# (docs/expo-sdk-upgrade-plan.md §6 参照。SDK アップグレード毎に要再評価)
legacy-peer-deps=true
